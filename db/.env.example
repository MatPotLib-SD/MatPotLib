# Supabase project (Dashboard -> Project Settings -> API)
SUPABASE_URL=https://REPLACE.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Optional: enables the LLM enrichment pass in import_miflora.ts.
# If absent, enrichment is skipped and the count of incomplete rows is logged.
OPENAI_API_KEY=
