// Copy this file to secrets.h (git-ignored) and fill in real values.
//
// WiFi credentials are NOT here any more — the device asks for them over its
// own captive portal ("PlantMonitor-Setup") on first boot, and remembers them.
// Hold the D2 button for 3 seconds to wipe them and reopen the portal.

// Backend base URL, no trailing slash. From DEPLOYMENT.md step 3.5.
#define API_URL      "https://REPLACE.azurecontainerapps.io"

// Per-pot identity, from DEPLOYMENT.md step 5 (devices + device_secrets rows).
#define DEVICE_ID    "REPLACE_UUID"
#define DEVICE_TOKEN "REPLACE_SECRET"
